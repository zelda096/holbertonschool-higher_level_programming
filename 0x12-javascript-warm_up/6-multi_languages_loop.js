#!/usr/bin/node
// this prints the array of strings that pass for the const

const array = ['C is fun', 'Python is cool', 'Javascript is amazing'];

for (let i = 0; i < 3; i++) {
  console.log(array[i]);
}
